/*! \page ConPredefinedGrammarDefinitions Готовые определения грамматических действий
 * 
 * Здесь приводится список всех \ref ConGrammar "готовых определений грамматических действий".
 * 
 * В описании готового определения \f$ def \f$ определяется *соответствующее действие*: грамматическое действие, содержащееся в грамматике, возвращаемой вызовом метода интерпретации, по индексу \f$ \varphi_a(def) \f$.
 * 
 * Определение успеха
 * --
 * 
 * Соответствующее действие: успех.
 * 
 * Определение неуспеха
 * --
 * 
 * Соответствующее действие: неуспех.
 * 
 * Имя `s`
 * --
 * 
 * `s` - это произвольная строка.
 * 
 * Соответствующее действие: перенаправление на индекс `i`, по которому располагается действие
 * - \f$ \varphi_n(s) \f$, если в определении грамматики содержится определение действия с именем `s`;
 * - неуспех, если имя `s` не соответствует ни одному определению действия, содержащемуся в определении грамматики.
 * 
 * Определение `def` с функцией отката `fdef`
 * --
 * 
 * `def` - это определение грамматического действия.
 * 
 * `fdef` - это \ref ConDataset "определение функции над совокупностью данных".
 * 
 * Соответствующее действие: перенаправление на индекс \f$ \varphi_a(def) \f$ с функцией отката `f`, получаемой согласно \ref ConDataset "рекомендованному способу интерпретации функций над совокупностью данных" с итоговым отображением имён данных \f$ \varphi_d \f$.
 * 
 * Определение регулярного выражения `r`
 * --
 * 
 * `r` - это произвольное регулярное выражение.
 * 
 * Соответствующее действие: регулярное выражение r.
 * 
 * Определение регулярного выражения r с именем данных `s`
 * --
 * 
 * `r` - это произвольное регулярное выражение.
 * 
 * `s` - это произвольная строка.
 * 
 * Соответствующее действие: регулярное выражение `r` с индексом данных \f$ \varphi_d(s) \f$.
 * 
 * Определение функции `fdef`
 * --
 * 
 * `fdef` - это \ref ConDataset "определение функции над совокупностью данных".
 * 
 * Соответствующее действие: функция `f`, получаемая согласно \ref ConDataset "рекомендованному способу интерпретации функций над совокупностью данных" с итоговым отображением имён данных \f$ \varphi_d \f$.
 * 
 * Определение альтернативы над (def1, ..., defk)
 * --
 * 
 * def1, ..., defk - это определения грамматических действий.
 * 
 * Соответствующее действие: альтернатива над \f$ (\varphi_a(def1), \dots, \varphi_a(defk)) \f$.
 * 
 * Определение правила над (def1, ..., defk)
 * --
 * 
 * def1, ..., defk - это определения грамматических действий.
 * 
 * Соответствующее действие: правило над \f$ (\varphi_a(def1), \dots, \varphi_a(defk)) \f$.
 * 
 * Опционал над определением `def`
 * --
 * 
 * `def` - это определение грамматического действия.
 * 
 * Соответствующее действие: опционал над \f$ \varphi_a(def) \f$.
 * 
 * Звезда Клини над определением `def`
 * --
 * 
 * `def` - это определение грамматического действия.
 * 
 * Соответствующее действие: звезда Клини над \f$ \varphi_a(def) \f$.
 */
