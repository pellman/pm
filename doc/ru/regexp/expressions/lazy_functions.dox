/*! \file include/pm/regexp/expressions/lazy_functions.hpp
 *  \brief Интерфейс создания \ref ConRegexp "ленивой части функциональных выражений".
 *  
 *  \ingroup ModuleRegexp
 *  
 *  Описание функции, возвращающей регулярное выражение, содержит
 *  - \ref ConRegexp "вид" возвращаемого выражения;
 *  - \ref ConPredefinedRegexps "название" возвращаемого выражения;
 *  
 *  Общее требование по использованию возвращаемых выражений:
 *  применение содержащейся \ref ConDataFunctions "функции над данными" согласно методу полного разбора выражения корректно.
 */

/*! \fn Regexp pm::regexp::acref(Fun && f)
 *  \brief Функциональное выражение: двуместная ссылочная функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::acren(Fun && f)
 *  \brief Функциональное выражение: двуместная переустанавливающая функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::acret(Fun && f)
 *  \brief Функциональное выражение: двуместная устанавливающая функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::asref(Fun && f)
 *  \brief Функциональное выражение: двуместная ссылочная функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::asren(Fun && f)
 *  \brief Функциональное выражение: двуместная переустанавливающая функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::asret(Fun && f)
 *  \brief Функциональное выражение: двуместная устанавливающая функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::mcref(Fun && f)
 *  \brief Функциональное выражение: одноместная ссылочная функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::mcren(Fun && f)
 *  \brief Функциональное выражение: одноместная переустанавливающая функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::mcret(Fun && f)
 *  \brief Функциональное выражение: одноместная устанавливающая функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::msref(Fun && f)
 *  \brief Функциональное выражение: одноместная ссылочная функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::msren(Fun && f)
 *  \brief Функциональное выражение: одноместная переустанавливающая функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::msret(Fun && f)
 *  \brief Функциональное выражение: одноместная устанавливающая функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::cref(AnyRet (*f)(Arg1, Arg2))
 *  \brief Функциональное выражение: двуместная ссылочная функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::cref(AnyRet (*f)(Arg))
 *  \brief Функциональное выражение: одноместная ссылочная функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::cren(Ret (*f)(Arg1, Arg2))
 *  \brief Функциональное выражение: двуместная переустанавливающая функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::cren(Ret (*f)(Arg))
 *  \brief Функциональное выражение: одноместная переустанавливающая функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::cret(Ret (*f)(Arg))
 *  \brief Функциональное выражение: двуместная устанавливающая функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::cret(Ret (*f)())
 *  \brief Функциональное выражение: одноместная устанавливающая функция \p f над текущими данными.
 */

/*! \fn Regexp pm::regexp::sref(AnyRet (*f)(Arg1, Arg2))
 *  \brief Функциональное выражение: двуместная ссылочная функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::sref(AnyRet (*f)(Arg))
 *  \brief Функциональное выражение: одноместная ссылочная функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::sren(Ret (*f)(Arg1, Arg2))
 *  \brief Функциональное выражение: двуместная переустанавливающая функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::sren(Ret (*f)(Arg))
 *  \brief Функциональное выражение: одноместная переустанавливающая функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::sret(Ret (*f)(Arg))
 *  \brief Функциональное выражение: двуместная устанавливающая функция \p f над сохранёнными данными.
 */

/*! \fn Regexp pm::regexp::sret(Ret (*f)())
 *  \brief Функциональное выражение: одноместная устанавливающая функция \p f над сохранёнными данными.
 */
