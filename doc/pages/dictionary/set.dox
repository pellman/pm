/*! \page DSet Множество
 *  
 *  
 *  *Множество* - это математическое и программистское понятие, строгое определение которого здесь не приводится, а вместо этого даётся следующее описание/пояснение.
 *  
 *  Множество в математическом смысле - это *коллекция* (*совокупность*) *элементов* S, такая что:
 *  - каждый элемент x может *входить* в S (x \f$ \in \f$ S) или *не входить* (x \f$ \notin \f$ S);
 *  - ничего другого сказать про элементы и множество нельзя: не задан порядок вхождения, отсутствует кратность, ...
 *  
 *  Стандартные обозначения и термины для множеств, используемые в документации:
 *  - *Пустое множество* не содержит ни одного элемента.
 *  - |S| - *размер* множества S: то, сколько элементов содержится в этом множестве.
 *  
 *  Множество в программистском смысле (в смысле C++) - это объект класса std::set.
 *  Этот объект - контейнер, содержащий конечную совокупность объектов (элементов) заданного типа, такую что любые два объекта, входящие в совокупность и различные по размещению в памяти, неравны.
 *  Для понимания документации достаточно иметь в виду, что объекты o1, o2 *равны*, если выражение "o1 == o2" возвращает true, и *неравны*, если оно возвращает false.
 *  Вообще говоря, равенство и неравенство объектов множества определяется чуть более нетривиально, но в текущем варианте библиотеки это неважно.
 */
