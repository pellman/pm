/*! \page DObjectModification Основные действия над объектами
 *  
 *  
 *  *Инициализация* объекта obj типа T от аргументов a1, ..., ak - это выполнение вызова T(a1, ..., ak) при создании объекта obj.
 *  Значение объекта запрещено использовать где бы то ни было до завершения инициализации.
 *  Тип T *инициализируем* от аргументов a1, ..., ak, если инициализация T(a1, ..., ak) корректна.
 *  
 *  *Инициализация по умолчанию* - это инициализация от пустого набора аргументов: T().
 *  
 *  *Копирование объекта* obj типа T - это инициализация нового объекта того же типа от выражения obj типа const T &: new T(obj).
 *  *Копия объекта* obj - это объект, инициализированный копированием от obj.
 *  Тип T *копируем*, если копирование объектов этого типа корректно.
 *  
 *  *Перемещение объекта* obj типа T - это
 *  - (процесс) инициализация нового объекта того же типа от выражения obj типа T &&: new T(obj);
 *  - (объект) объект, инициализированный перемещением от obj.
 *  
 *  Тип T *перемещаем*, если перемещение объектов этого типа корректно.
 *  
 *  *Копирующее присваивание* для типа T - это присваивание (=), левая часть которого имеет тип T &, а правая - const T &.
 *  Тип T *копируем через присваивание*, если копирующее присваивание для этого типа корректно
 *  
 *  *Перемещающее присваивание* для типа T - это присваивание (=), левая часть которого имеет тип T &, а правая - T &&.
 *  Тип T *перемещаем через присваивание*, если перемещающее присваивание для этого типа корректно.
 *  
 *  *Уничтожение объекта* типа T - это выполнение вызова ~T() - как правило, с освобождением памяти, занимаемой объектом.
 *  Объект запрещено использовать где бы то ни было после начала уничтожения.
 *  Тип T *уничтожим*, если уничтожение этого типа корректно.
 */
