/*! \page DRangeBasedFor Цикл по коллекции
 *  
 *  
 *  *Цикл по коллекции* - это команда вида `for(T var : collection) {...}`, введённая в стандарте C++11 и предназначенная для последовательного перебора всех элементов контейнера collection в переменной var.
 *  
 *  В документации тип называется *совместимым с циклом по коллекции*, если объект этого типа можно использовать справа от двоеточия в цикле по коллекции.
 *  
 *  *Перебор элементов* в переменной var - это последовательная запись (перед выполнением каждого витка цикла) в переменную var ссылок на элементы контейнера collection или копий значений этих элементов (в зависимости от наличия ссылок в типе T).
 */
