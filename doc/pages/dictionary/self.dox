/*! \page DSelf Self
 *  
 *  
 *  Для каждого объекта определён указатель this на этот объект, и ссылка на сам объект получается разыменованием этого указателя: \*this.
 *  Надпись "*this" в документации смотрится не очень хорошо, так что вместо неё используется *self* (как в python).
 */
