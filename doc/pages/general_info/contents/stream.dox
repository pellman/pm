/*! \page GICStream Модуль возвратных потоков
 *  
 *  Этим модулем предоставляется класс \ref pm::stream::Stream "буферизованного потока" - \ref GNNUngettableStream "возвратного потока" для которого, в числе прочего, определены:
 *  - создание буферизованного потока, "обёрнутого" над \ref GNSTypes "стандартным потоком ввода", то есть содержащего
 *   - этот стандартный поток и
 *   - особый буфер, в который возвращаются \ref GNSTypes "символы" (и \ref GNSTypes "строки");
 *  - чтение символов и строк (в терминах стандартных потоков - неформатированное);
 *  - возврат символов и строк обратно в поток: по аналогии с методами unget и putback стандартных потоков ввода, но при этом с гарантированным успехом.
 */
