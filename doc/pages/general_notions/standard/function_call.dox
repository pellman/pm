/*! \page GNSFunctionCall Функции и функциональные объекты
 *  
 *  *Вызов функции* (или любого функционального объекта) `f` с аргументами `a1, ..., ak` - это выражение `f(a1, ..., ak)`.
 *  
 *  *Момент вызова* - это момент времени выполнения программы, в который следующим выполняется вызов.
 */
