/*! \page GNNParsing Процедура разбора
 *  
 *  *Процедура разбора* принимает на вход \ref GNNUngettableStream "возвратный поток" \f$ s \f$ и специальную структуру данных \f$ c \f$ - *контекст разбора* - и производит следующие действия:
 *  - чтение и возврат символов из/в \f$ s \f$;
 *  - извлечение \ref GNNData "полезной информации" по итогам чтения символов:
 *   - вид извлекаемой информации определеяется структурой \f$ c \f$;
 *   - извлечённая информация записывается в \f$ c \f$;
 *  - обязательно в качестве последнего действия выдаётся *итог разбора*: \ref GNSTypes "флаг", поднятый при положительном итоге (*успехе*) и опущенный при отрицательном итоге (*неуспехе*).
 *  
 *  В случае успеха процедурой разбора также определяется *принятая строка*.
 *  Это \ref GNSTypes "строка", как правило состоящая из \ref GNSTypes "символов", прочитанных из потока в конечном итоге (за вычетом всех возвратов).
 *  
 *  В библиотеке есть два понятия, использующие процедуру разбора: \ref pm::regexp::Regexp "регулярные выражения" и \ref pm::grammar::Grammar "грамматики".
 *  Им соответствуют \ref pm::regexp::Context "регулярный контекст" и \ref pm::grammar::Context "грамматический контекст", которыми - каждым по-своему - определяются возможности извлечения полезной информации в процессе разбора.
 */
