/*! \file include/pm/stream/position.hpp
 *  \brief Содержит класс позиций \ref pm::stream::Stream "потока разбора" pm::stream::Position.
 *  \ingroup ModuleStream
 */

/*! \class pm::stream::Position
 *  \brief Класс позиций \ref pm::stream::Stream "потоков разбора".
 *  \ingroup ModuleStream
 *  
 *  
 *  Позиция потока разбора - это \ref DTypes "индекс", принадлежащий одному из \ref StreamCluster "кластеров" потоков разбора.
 *  
 *  Особые свойства класса
 *  ==
 *  
 *  \ref DValidState "Нестандартно-корректен".
 *  
 *  Неявные методы
 *  ==
 *  
 *      Position()
 *  
 *  Удалён.
 *  
 *  
 *      Position(const Position & pos)
 *      Position(Position && pos)
 *  
 *  Инициализировать позицию, равную \p pos и принадлежащую тому же кластеру, что и \p pos.
 *  
 *  
 *      Position & operator =(const Position & pos)
 *      Position & operator =(Position && pos)
 *  
 *  Переместить \ref DSelf "self" в кластер позиции \p pos и перезаписать её значением \p pos.
 *  
 *  \return Self.
 *  
 *  
 *      ~Position()
 *  
 *  Уничтожить позицию.
 */

/*! \fn bool pm::stream::utils::Position::operator ==(const Position & pos) const
 *  \brief Обычное сравнение индексов.
 */

/*! \fn bool pm::stream::utils::Position::operator !=(const Position & pos) const
 *  \brief Обычное сравнение индексов.
 */

/*! \fn bool pm::stream::utils::Position::operator >(const Position & pos) const
 *  \brief Обычное сравнение индексов.
 */

/*! \fn bool pm::stream::utils::Position::operator <(const Position & pos) const
 *  \brief Обычное сравнение индексов.
 */

/*! \fn bool pm::stream::utils::Position::operator >=(const Position & pos) const
 *  \brief Обычное сравнение индексов.
 */

/*! \fn bool pm::stream::utils::Position::operator <=(const Position & pos) const
 *  \brief Обычное сравнение индексов.
 */

/*! \fn size_t pm::stream::utils::Position::operator -(const Position & pos) const
 *  \brief Обычная разность индексов.
 *  
 *  \pre \p pos <= \ref DSelf "self".
 */
