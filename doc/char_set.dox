/*! \file include/pm/char_set.hpp
 *  \brief Содержит класс буквенных множеств pm::CharSet.
 */

/*! \class pm::CharSet
 *  \brief Класс буквенных множеств.
 *  
 *  *Буквенное множество* - это просто \ref DSet "множество" \ref DTypes "стандартных символов", взаимодействие с которым ограничено одним лишь \ref pm::CharSet::contains "методом проверки принадлежности символа".
 *  
 *  Для удобного создания буквенных множеств библиотекой предоставляется \ref CCharSet "соответствующий интерфейс".
 *  
 *  Особые свойства класса
 *  ==
 *  
 *  \ref DValidState "Нестандартно-корректен".
 *  
 *  Специальные методы
 *  ==
 *  
 *      CharSet()
 *  
 *  Инициализировать пустое множество.
 *  
 *  
 *      CharSet(const CharSet & r)
 *      CharSet(CharSet && r)
 *  
 *  Инициализировать множество, состоящее из тех же символов, что и \p r.
 *  
 *  
 *      CharSet & operator =(const CharSet & r)
 *      CharSet & operator =(CharSet && r)
 *  
 *  Сделать \ref DSelf "self" множеством тех же символов, что и \p r.
 *  
 *  \return Self.
 *  
 *  
 *      ~CharSet()
 *  
 *  Уничтожить множество.
 */

/*! \fn bool pm::CharSet::contains(char c) const
 *  \brief Вернуть true <=> символ \p c содержится в множестве.
 *  
 *  Этот метод устроен по-своему для каждого конкретного множества, и его устройство - основная часть документации интерфейса создания буквенных множеств.
 */
