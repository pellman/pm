/*! \file include/pm/grammar/syntax/actions/star.hpp
 *  \brief Содержит интерфейс создания определений грамматического действия \ref PGStar "звезды Клини".
 *  \ingroup ModuleGrammar
 *  \anchor PGDStar
 *  
 *  Определение звезды Клини - это \ref pm::grammar::syntax::Action "определение грамматического действия", устроенное так:
 *  - Оно содержит определение грамматического действия \p a.
 *  - \ref pm::grammar::syntax::Grammar "Результат интерпретации": \ref PGStar "звезда Клини" над индексом \f$ \varphi_i(a) \f$, где \f$ \varphi_i \f$ - отображение определений грамматических действий в индексы, определяемое \ref pm::grammar::syntax::Grammar "способом интерпретации определения грамматики".
 *  
 *  Описанное определение возвращается вызовом функции или оператора интерфейса.
 *  
 *  \pre Определение звезды Клини, возвращаемое вызовом функции или оператора интерфейса, не используется (прямо или косвенно) в определении \p a.
 *  
 *  \sa \ref PGList "Список готовых грамматических действий и их определений"
 */

/*! \fn Action pm::grammar::syntax::star(const Action & a)
 */

/*! \fn Action pm::grammar::syntax::star(Action && a)
 */

/*! \fn Action pm::grammar::syntax::operator *(const Action & a)
 */

/*! \fn Action pm::grammar::syntax::operator *(Action && a)
 */
