/*! \file include/pm/parser.hpp
 *  \brief Содержит класс парсеров pm::Parser.
 */

/*! \class pm::Parser
 *  \brief Класс парсеров.
 *  
 *  
 *  *Парсеры* предназначены для \ref Intro "разбора текстов" с доступом к этим текстам через \ref pm::Stream "потоки разбора" и с извлечением полезных данных в рамках \ref pm::Context "контекстов".
 *  
 *  Для удобного создания парсеров библиотекой предостваляется \ref CParser "соответствующий интерфейс".
 *  
 *  В сложных случаях может быть полезно создание подходящего парсера с помощью \ref pm::ParSystem "класса систем парсеров".
 *  
 *  Особые свойства класса
 *  ==
 *  
 *  \ref DValidState "Нестандартно-корректен".
 *  
 *  Неявные методы
 *  ==
 *  
 *      Parser()
 *  
 *  Инициализировать парсер \ref CPfalse "Неуспех".
 *  
 *  
 *      Parser(const Parser & par)
 *      Parser(Parser && par)
 *  
 *  Инициализировать парсер с таким же методом разбора, как и у \p par в момент инициализации.
 *  
 *  
 *      Parser & operator =(const Parser & par)
 *      Parser & operator =(Parser && par)
 *  
 *  Сделать метод разбора \ref DSelf "self" таким же, как и метод разбора \p par в момент присваивания.
 *  
 *  \return Self.
 *  
 *  
 *      ~Parser()
 *  
 *  Уничтожить парсер.
 */

/*! \fn bool pm::Parser::parse(Stream & is, Context & con) const
 *  \brief Метод (\p is, \p con)-разбора: разбора потока \p is в контексте \p con.
 *  
 *  Этот метод устроен по-своему для каждого конкретного парсера, и его устройство - основная часть документации интерфейса создания парсеров.
 *  
 *  \return *Итог разбора*: true <=> разбор *успешен*.
 */
